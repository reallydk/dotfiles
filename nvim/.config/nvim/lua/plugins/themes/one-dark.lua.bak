return {
  "olimorris/onedarkpro.nvim",
  priority = 1000,
  config = function()
    require("onedarkpro").setup({
      options = {
        transparency = true,
      },
      plugins = {
        all = true,
      },
    })
    -- vim.cmd("colorscheme onedark")
  end,
}
